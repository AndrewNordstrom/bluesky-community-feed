name: issue-to-linear

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write

jobs:
  issue-to-linear:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/issue-to-linear.yml@62bbabdf9e01fc8bfa825101beeb4aa3c388be55
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
