name: secret-scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  secret-scan:
    # Pinned to the org .github commit that includes secret-scan reusable workflow.
    uses: andrewnordstrom-eng/.github/.github/workflows/secret-scan.yml@384b05853d9fdde266479e2ebe23112819d0269b
